{
  "name": "roundaround-functions",
  "version": "2.0.0",
  "description": "Cloud Functions for Round Around: JaaS voice-chat tokens and share-link shortening",
  "main": "index.js",
  "private": true,
  "license": "ISC",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "node --test",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2",
    "jsonwebtoken": "^9.0.3"
  }
}
