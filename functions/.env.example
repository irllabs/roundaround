# Non-secret parameters read by firebase-functions params on deploy.
# Copy to .env.<project-id> (e.g. .env.roundaround) and adjust.
JAAS_APP_ID=vpaas-magic-cookie-6e18748a3e614d5696744abf547dd11c
JAAS_KEY_ID=vpaas-magic-cookie-6e18748a3e614d5696744abf547dd11c/ec8a8c
BITLY_GROUP_GUID=
APP_ORIGIN=https://rounds.studio
# Secrets are NOT read from this file. Set them once with:
#   firebase functions:secrets:set JAAS_PRIVATE_KEY   (paste the contents of the .pk file)
#   firebase functions:secrets:set BITLY_TOKEN
